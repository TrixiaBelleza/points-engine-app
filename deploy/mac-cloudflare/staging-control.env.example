# Copy to staging-control.env (gitignored), then generate a real token:
#   openssl rand -hex 32
# The Environment Setup Agent secret is this raw value only — no "Bearer " prefix.
STAGING_CONTROL_TOKEN=replace-with-openssl-rand-hex-32
